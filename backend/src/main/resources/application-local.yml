springdoc:
  api-docs:
    enabled: ${OPENAPI_ENABLED:true}
  swagger-ui:
    enabled: ${OPENAPI_ENABLED:true}

management:
  health:
    mail:
      enabled: false

logging:
  level:
    pe.edu.upt.fuerzaupt: INFO
